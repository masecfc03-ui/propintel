name: Keep API Alive
on:
  schedule:
    - cron: '*/10 * * * *'  # every 10 minutes
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping PropIntel API
        run: curl -s https://propintel-htij.onrender.com/health