services:
  - type: web
    name: deallens-api
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 10000
      - key: DEBUG
        value: false
    plan: free
